# 上游 API Key（九天 / OpenRouter / One API 等均填此处）
UPSTREAM_API_KEY=your_api_key_here

# 兼容旧配置名（可选，二选一）
# JIUTIAN_API_KEY=

# 设为 true 时优先使用 Cursor 传入的 Authorization Bearer
USE_CLIENT_AUTH=false

# 可选：使用非默认配置文件（一般保持注释，直接改 config.yaml 即可）
# PROXY_CONFIG=config.yaml
