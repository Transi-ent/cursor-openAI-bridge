# 配置参考模板

[English](README.en.md)

**不要**用脚本覆盖 `config.yaml`。请打开对应 yaml 查阅字段含义，**手工合并**到项目根目录的 [`config.yaml`](../config.yaml)。

切换模型：只改 `config.yaml` 里的 `model_aliases`。  
切换上游：只改 `config.yaml` 里的 `provider` 与 `upstream`。
