# Configuration reference templates

**Do not** overwrite `config.yaml` with these files. Open a template to see field meanings, then **merge manually** into [`config.yaml`](../config.yaml) at the project root.

- **Switch models**: edit `model_aliases` in `config.yaml`
- **Switch upstream**: edit `provider` and `upstream` in `config.yaml`
